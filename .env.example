# Environment Configuration

# Server
PORT=3001
NODE_ENV=development

# Redis (for session storage)
# Development: use local Redis
# Production: use Redis Cloud or similar
REDIS_URL=redis://localhost:6379

# App Base URL (for generating invite links)
APP_URL=http://localhost:3000

# TURN Server Configuration (for WebRTC)
# Use these free TURN servers for testing, replace with your own in production
TURN_SERVER_URL=turn:openrelay.metered.ca:80
TURN_USERNAME=openrelayproject
TURN_CREDENTIAL=openrelayproject

# Alternative: Use Twilio TURN servers (more reliable for production)
# TURN_SERVER_URL=turn:global.turn.twilio.com:3478?transport=tcp
# TURN_USERNAME=your_twilio_username
# TURN_CREDENTIAL=your_twilio_credential

# STUN Servers (for NAT traversal)
STUN_SERVER_URL=stun:stun.l.google.com:19302

# Session Configuration
SESSION_TTL_MINUTES=30

# CORS Origins (comma-separated)
CORS_ORIGINS=http://localhost:3000,http://localhost:5173,https://your-vercel-app.vercel.app
